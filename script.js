const $=(s,c=document)=>c.querySelector(s);const $$=(s,c=document)=>[...c.querySelectorAll(s)];const header=$("#siteHeader"),toggle=$("#menuToggle"),menu=$("#mobileMenu");function setMenu(open){if(!toggle||!menu)return;toggle.classList.toggle("active",open);toggle.setAttribute("aria-expanded",String(open));toggle.setAttribute("aria-label",open?"Close menu":"Open menu");menu.hidden=!open;document.body.classList.toggle("menu-open",open)}function updateHeader(){header?.classList.toggle("scrolled",window.scrollY>10)}updateHeader();window.addEventListener("scroll",updateHeader,{passive:true});toggle?.addEventListener("click",()=>setMenu(menu?.hidden));$$("a",menu).forEach(a=>a.addEventListener("click",()=>setMenu(false)));window.addEventListener("keydown",e=>{if(e.key==="Escape")setMenu(false)});window.addEventListener("resize",()=>{if(window.innerWidth>820)setMenu(false)});const year=$("#year");if(year)year.textContent=new Date().getFullYear();
